# Local development: API requests go directly to the backend
VITE_API_URL=http://localhost:3001

# In production, API calls use relative paths (/api/*) and nginx proxies them
# to the backend container. No VITE_API_URL is needed in production.
